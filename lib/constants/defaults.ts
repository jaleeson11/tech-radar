// Default quadrant names for technology radar
export const DEFAULT_QUADRANTS = [
  'Tools',
  'Techniques',
  'Platforms',
  'Languages & Frameworks',
];

// Default ring names for technology radar (from inner to outer)
export const DEFAULT_RINGS = [
  'Adopt',    // Inner ring - technologies we have high confidence in
  'Trial',    // Second ring - worth pursuing
  'Assess',   // Third ring - worth exploring
  'Hold',     // Outer ring - proceed with caution
];

// Maximum number of tech items per radar
export const MAX_ITEMS_PER_RADAR = 200;

// Maximum number of radars per user
export const MAX_RADARS_PER_USER = 10;
